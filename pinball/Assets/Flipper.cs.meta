using UnityEngine;
using System.Collections;

public class Pinball : MonoBehaviour {

	private float pinballDeathHeight = -2.0f; 

	void Update () {
		if(this.gameObject.transform.position.y < pinballDeathHeight){
			Destroy (this.gameObject);
		}
	}
}
